 .name fcn.00007240
 .offset 0000000000007240
 .file sha256sum
 push rbp
 mov rbp, rdi
 push rbx
 mov ebx, esi
 sub rsp, CONST
 call CONST
 movzx edx, byte [rax]
 and edx, CONST
 cmp dl, CONST
 cjmp LABEL9
 movzx edx, byte [rax + CONST]
 and edx, CONST
 cmp dl, CONST
 cjmp LABEL13
 movzx edx, byte [rax + CONST]
 and edx, CONST
 cmp dl, CONST
 cjmp LABEL13
 cmp byte [rax + CONST], CONST
 cjmp LABEL13
 cmp byte [rax + CONST], CONST
 cjmp LABEL13
 cmp byte [rax + CONST], CONST
 cjmp LABEL13
 cmp byte [rbp], CONST
 lea rax, [rip + CONST]
 lea rdx, [rip + CONST]
 cmovne rax, rdx
 add rsp, CONST
 pop rbx
 pop rbp
 ret
LABEL9:
 cmp dl, CONST
 cjmp LABEL13
 movzx edx, byte [rax + CONST]
 and edx, CONST
 cmp dl, CONST
 cjmp LABEL37
LABEL13:
 cmp ebx, CONST
 lea rax, [rip + CONST]
 lea rdx, [rip + CONST]
 cmovne rax, rdx
 add rsp, CONST
 pop rbx
 pop rbp
 ret
LABEL37:
 cmp byte [rax + CONST], CONST
 cjmp LABEL13
 cmp byte [rax + CONST], CONST
 cjmp LABEL13
 cmp byte [rax + CONST], CONST
 cjmp LABEL13
 cmp byte [rax + CONST], CONST
 cjmp LABEL13
 cmp byte [rax + CONST], CONST
 cjmp LABEL13
 cmp byte [rax + CONST], CONST
 cjmp LABEL13
 cmp byte [rbp], CONST
 lea rax, [rip + CONST]
 lea rdx, [rip + CONST]
 cmovne rax, rdx
 add rsp, CONST
 pop rbx
 pop rbp
 ret
